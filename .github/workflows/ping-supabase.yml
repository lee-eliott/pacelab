name: Ping Supabase

on:
  schedule:
    - cron: "0 12 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase via API route
        run: curl -f https://pacelab-five.vercel.app/api/cron/ping
